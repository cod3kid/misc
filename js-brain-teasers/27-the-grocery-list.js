const groceryList = [];

groceryList[0] = "Bread";
groceryList[1] = "Milk";
groceryList["user"] = "John";

console.log(groceryList.length);

/*
Outputs 2 because user is just a property we attach to the array,
it' not an element of the array
*/
